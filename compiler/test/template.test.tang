var person = {
    name: 'Yang Chi',
    age:27,
    "position{}": "ceo",
    language:["php", 'js']
}
var osi = {
    s: 'osi'
};
var s = 's';
console.log(`       Hello, ${person.name},
    you {} are ${person['age']} years old,
    you {are} a ${person["position{}"]},
    you {are} a ${person[`p${osi[`${s}`]}tion{}`]},
    you like ${person.language[1]}.
    it's a ${{test:'test'}.test} code.
`);