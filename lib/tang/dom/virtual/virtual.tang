@module;

use $ dom/virtual/Component;

namespace dom;

public virtual = (options={}){
    return new virtual.Component(options);
};

public createVElement (tagName, props, children) {
    return new virtual.Element(tagName, props, children);
}
