use $ dom/Template;


let document = root.document,
view = document.getElementById('view'),
template = @template 'tpl';
app.innerHTML = template.complie({
    greeting: 'Hello',
    name: 'tanguage',
}).echo();
console.log(template);